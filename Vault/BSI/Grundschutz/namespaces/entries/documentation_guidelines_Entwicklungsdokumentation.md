---
tags:
- BSI
- autogenerated
- namespace_entry
- Namespace
created: Sun Apr 12 22:01:09 CEST 2026
namespace: documentation_guidelines
---


# [[documentation_guidelines]] &rarr; Entwicklungsdokumentation

| | |
| --- | --- |
| ** UUID ** |  | 


Enthält für eine selbst entwickelte Software die Architektur, User Stories, Bedienführung, etc.




| Spalte | Wert |
|-----|-------|
| ** Begriff ** | Entwicklungsdokumentation |
| ** Definition ** | Enthält für eine selbst entwickelte Software die Architektur, User Stories, Bedienführung, etc. |
| ** Kategorie ** | Entwicklung & Beschaffung |
| ** Zielgruppe ** | Prozessverantwortliche |
| ** bisher im IT-Grundschutz ** |   |
### Controls mit "Entwicklungsdokumentation" Dokumentation 

```dataviewjs
let pages = dv.pages()
  .where(p => p.stmt_documentation == "Entwicklungsdokumentation" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [[documentation_guidelines]] at Sun Apr 12 22:01:05 CEST 2026 _
