---
tags:
- BSI
- autogenerated
- namespace_entry
created: Sat May 02 11:42:30 CEST 2026
namespace: documentation_guidelines
namespace_entry: Risikobewertung
---


# [documentation_guidelines](../documentation_guidelines.md) &rarr; Risikobewertung

| | |
| --- | --- |
| ** UUID ** |  |

Ergebnis der Risikoanalyse: Dieses Dokument (oder ein Dashboard in einem Tool) enthält die identifizierten Risiken, deren Bewertung (Eintrittswahrscheinlichkeit und Schadensausmaß) und die Entscheidung zur Risikobehandlung (z.B. mindern, akzeptieren, vermeiden, transferieren). Es ist die Entscheidungsgrundlage für die Priorisierung von Sicherheitsmaßnahmen.

| Spalte | Wert |
|-----|-------|
| ** Begriff ** | Risikobewertung |
| ** Definition ** | Ergebnis der Risikoanalyse: Dieses Dokument (oder ein Dashboard in einem Tool) enthält die identifizierten Risiken, deren Bewertung (Eintrittswahrscheinlichkeit und Schadensausmaß) und die Entscheidung zur Risikobehandlung (z.B. mindern, akzeptieren, vermeiden, transferieren). Es ist die Entscheidungsgrundlage für die Priorisierung von Sicherheitsmaßnahmen. |
| ** Kategorie ** | Risikomanagement |
| ** Zielgruppe ** | Prozessverantwortliche |
| ** bisher im IT-Grundschutz ** | Ergebnisse Risikoanalyse |
### Controls mit "Risikobewertung" Dokumentation

```dataviewjs
let pages = dv.pages()
  .where(p => p.stmt_documentation == "Risikobewertung" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [documentation_guidelines](../documentation_guidelines.md) at Sat May 02 11:42:30 CEST 2026 _
