---
tags:
- namespace_entry
- BSI
- autogenerated
bsi_type: namespace_entry
created: Thu May 07 15:19:57 CEST 2026
namespace: effort_level
namespace_entry: '0'
bsi_title: effort_level - 0
---


# [effort_level](../effort_level.md) &rarr; 0

| | |
| --- | --- |
| ** UUID ** |  |

Der Aufwand der Anforderung wird nicht bewertet, da ihre Implementierung in jedem Fall zwingend erforderlich ist. Beispiel: Benennung eines Informationssicherheitsbeauftragten.

| Spalte | Wert |
|-----|-------|
| ** Aufwand ** | 0 |
| ** Definition ** | Der Aufwand der Anforderung wird nicht bewertet, da ihre Implementierung in jedem Fall zwingend erforderlich ist. Beispiel: Benennung eines Informationssicherheitsbeauftragten. |
### Controls mit Aufwand 0

```dataviewjs
let pages = dv.pages()
  .where(p => p.control_effort_level == "0" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [effort_level](../effort_level.md) at Thu May 07 15:19:57 CEST 2026 _
