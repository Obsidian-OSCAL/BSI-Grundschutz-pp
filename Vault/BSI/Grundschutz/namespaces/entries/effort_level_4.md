---
tags:
- BSI
- autogenerated
- namespace_entry
- Namespace
created: Sun Apr 12 22:01:09 CEST 2026
namespace: effort_level
---


# [[effort_level]] &rarr; 4

| | |
| --- | --- |
| ** UUID ** |  | 


In der Regel sind diese Anforderungen mit einer längerfristigen Umsetzung oder Beibehaltung verbunden. Es wird Expertenwissen und oftmals auch eine Unterstützung von Dritten benötigt. Für die Aufrechterhaltung ist häufig ein größeres Team von Betriebspersonal erforderlich. Beispiel: Initiierung und Umsetzung von Baumaßnahmen.




| Spalte | Wert |
|-----|-------|
| ** Aufwand ** | 4 |
| ** Definition ** | In der Regel sind diese Anforderungen mit einer längerfristigen Umsetzung oder Beibehaltung verbunden. Es wird Expertenwissen und oftmals auch eine Unterstützung von Dritten benötigt. Für die Aufrechterhaltung ist häufig ein größeres Team von Betriebspersonal erforderlich. Beispiel: Initiierung und Umsetzung von Baumaßnahmen. |
### Controls mit Aufwand 4

```dataviewjs
let pages = dv.pages()
  .where(p => p.control_effort_level == "4" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [[effort_level]] at Sun Apr 12 22:01:05 CEST 2026 _
