---
tags:
- BSI
- autogenerated
- namespace_entry
- Namespace
created: Sun Apr 12 22:01:09 CEST 2026
namespace: effort_level
---


# [[effort_level]] &rarr; 5

| | |
| --- | --- |
| ** UUID ** |  | 


In der Regel sind für die Umsetzung aufwändige, komplexe Maßnahmen oder eine individuelle Abwägung und Behandlung damit verbundener sekundärer Risiken erforderlich. Hierzu ist häufig tiefgehendes Expertenwissen oder der Einsatz entsprechender externer Dienstleister, sowie eine sorgfältige Planung  und Aufrechterhaltung notwendig. Beispiel: Aufbau georedundanter Rechenzentren.




| Spalte | Wert |
|-----|-------|
| ** Aufwand ** | 5 |
| ** Definition ** | In der Regel sind für die Umsetzung aufwändige, komplexe Maßnahmen oder eine individuelle Abwägung und Behandlung damit verbundener sekundärer Risiken erforderlich. Hierzu ist häufig tiefgehendes Expertenwissen oder der Einsatz entsprechender externer Dienstleister, sowie eine sorgfältige Planung  und Aufrechterhaltung notwendig. Beispiel: Aufbau georedundanter Rechenzentren. |
### Controls mit Aufwand 5

```dataviewjs
let pages = dv.pages()
  .where(p => p.control_effort_level == "5" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [[effort_level]] at Sun Apr 12 22:01:05 CEST 2026 _
