---
tags:
- namespace_entry
- BSI
- autogenerated
bsi_type: namespace_entry
created: Thu May 07 15:19:57 CEST 2026
namespace: security_level
namespace_entry: normal-SdT
bsi_title: security_level - normal-SdT
---


# [security_level](../security_level.md) &rarr; normal-SdT

| | |
| --- | --- |
| ** UUID ** |  |

Standard-Sicherheitsstufe

| Spalte | Wert |
|-----|-------|
| ** Begriff ** | normal-SdT |
| ** Definition ** | Standard-Sicherheitsstufe |
### normal-SdT Controls

```dataviewjs
let pages = dv.pages()
  .where(p => p.control_security_level == "normal-SdT" &&
              p.file.etags.includes("#oscal_control"))
  .sort(p => p.file.name)
  .groupBy(p => p.file.folder)
  .sort(g => g.key);

let rows = [];

for (let group of pages) {
    let first = true;
    for (let page of group.rows) {
        rows.push([
            first ? group.key : "",
            page.file.link
        ]);
        first = false;
    }
}

dv.table(
    ["Verzeichnis", "Control"],
    rows
);

```
_ created from [security_level](../security_level.md) at Thu May 07 15:19:57 CEST 2026 _
